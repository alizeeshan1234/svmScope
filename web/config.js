// Which svmscope engine this frontend talks to.
// The UI also accepts ?api=<url> at runtime to point at a different instance
// (e.g. http://127.0.0.1:3000 when you're running the engine locally).
window.SVMSCOPE_API = "https://svmscope.onrender.com";
